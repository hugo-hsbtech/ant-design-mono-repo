// Re-export the full @ant-design/icons set so apps have one icon source...
export * from '@ant-design/icons';

// ...plus our own brand icons and the helper to build more.
export { createIcon, type IconProps } from './createIcon';
export { WaffleIcon } from './WaffleIcon';
