import { Grid } from 'antd';

export { useThemeMode } from '../providers/ThemeProvider';

/** antd responsive breakpoint hook, re-exported for convenience. */
export const useBreakpoint = Grid.useBreakpoint;
